#include <stdio.h>
#include <string.h>
#include "common.h"

void* run_sender(void* arg) {
    printf("Sender iniciado.\n");
    //printf("ID: %d\n", neighbors);

    // while(1) {
    //     // Lógica para enviar pacotes
    // }
    return NULL;
}